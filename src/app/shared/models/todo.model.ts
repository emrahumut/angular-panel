export interface TodoRequestModel {
    userId: number;
    id: number;
    title: string;
    completed: boolean;
}
export interface TodoModel {
    username: string;
    title: string;
    completed: boolean;
}