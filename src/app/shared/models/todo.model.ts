export interface TodoModel {
    userId: number;
    id: number;
    title: string;
    completed: boolean;
}
export interface TodoTableModel {
    username: string;
    title: string;
    completed: boolean;
}